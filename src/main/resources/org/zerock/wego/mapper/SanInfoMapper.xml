<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.zerock.wego.mapper.SanInfoMapper">

	<select id="selectAll"
		resultType="org.zerock.wego.domain.info.SanInfoViewVO">
		SELECT * 
		FROM san_info_v
	</select>
	
	<select id="selectAllByAbc"
		resultType="org.zerock.wego.domain.info.SanInfoViewSortVO">
		SELECT * 
		FROM san_info_v_abc
		<if test="lastItemId != null">
        	WHERE sort_num &gt; #{lastItemId}
		</if>
   		OFFSET ( #{page} - 1 ) * #{amount} ROWS
      	FETCH NEXT #{amount} ROWS ONLY
	</select>
	
		<select id="selectAllByLike"
		resultType="org.zerock.wego.domain.info.SanInfoViewSortVO">
		SELECT * 
		FROM san_info_v_like
		<if test="lastItemId != null">
        	WHERE sort_num &gt; #{lastItemId}
		</if>
   		OFFSET ( #{page} - 1 ) * #{amount} ROWS
      	FETCH NEXT #{amount} ROWS ONLY
	</select>
	
	<select id="selectSearchSanInfoByQuery"
		resultType="org.zerock.wego.domain.info.SanInfoViewVO">
		SELECT *
		FROM san_info_v
		WHERE
			(
			san_name LIKE '%'||#{search}||'%'
			OR details LIKE '%'||#{search}||'%'
			)
	</select>

	<select id="selectRandom10"
		resultType="org.zerock.wego.domain.info.SanInfoViewVO">
		SELECT *
		FROM
			(
			SELECT *
			FROM san_info_v
			ORDER BY dbms_random.random
			)
		WHERE rownum &lt;= 10
	</select>
	
	<select id="selectSearchSanInfo3ByQuery"
		resultType="org.zerock.wego.domain.info.SanInfoViewVO">
		SELECT *
		FROM san_info_v
		WHERE
			(
			san_name LIKE '%'||#{search}||'%'
			OR details LIKE '%'||#{search}||'%'
			)
			AND rownum &lt;= 3
	</select>

	<select id="selectById"
		resultType="org.zerock.wego.domain.info.SanInfoViewVO">
		SELECT *
		FROM san_info_v
		WHERE san_info_id = #{sanInfoId}
	</select>

	<select id="selectIdBySanName" resultType="Integer">
		SELECT san_info_id
		FROM san_info_v
		WHERE san_name = #{sanName}
	</select>

</mapper>